package trente.asia.android.util;

/**
 * <strong>CCSerializeObjectUtil</strong><br>
 * <br>
 *
 * @author TrungND
 * @version $Id$
 */
public class CCDisabledObjectUtil{

	// private static int DEFAULT_COLOR_EDITTEXT = Color.parseColor("#F0F0F0");

	/**
	 * <strong>disabledObject</strong><br>
	 * <br>
	 *
	 * @return
	 */
	// public static void disabledObject(Context context, ViewGroup layout, Map<String, String> vc) {
	// if (layout == null) {
	// return;
	// }
	//
	// for (int i = 0; i < layout.getChildCount(); i++) {
	// View view = layout.getChildAt(i);
	// if (view instanceof ChiaseEditText) {
	// String viewControl = ((ChiaseEditText) view).getViewControl();
	// if (CCConst.ENABLED.equals(viewControl) || CCConst.ENABLED.equals(vc.get(viewControl))) {
	// EditText defaultEditText = new EditText(context);
	// view.setEnabled(true);
	// view.setBackground(defaultEditText.getBackground());
	// ((EditText) view).setCursorVisible(true);
	// ((EditText) view).setKeyListener(defaultEditText.getKeyListener());
	// } else {
	// view.setEnabled(false);
	// view.setBackgroundColor(Color.TRANSPARENT);
	// ((EditText) view).setCursorVisible(false);
	// ((EditText) view).setKeyListener(null);
	// ((EditText) view).setMinHeight(50);
	// }
	// } else if (view instanceof ChiaseSpinner) {
	// String viewControl = ((ChiaseSpinner) view).getViewControl();
	// if (CCConst.ENABLED.equals(viewControl) || CCConst.ENABLED.equals(vc.get(viewControl))) {
	// Spinner defaultSpinner = new Spinner(context);
	// view.setEnabled(true);
	// view.setBackground(defaultSpinner.getBackground());
	// } else {
	// view.setEnabled(false);
	// view.setBackgroundColor(Color.TRANSPARENT);
	// }
	// } else if (view instanceof ChiaseButton) {
	// String viewControl = ((ChiaseButton) view).getViewControl();
	// if (CCConst.ENABLED.equals(viewControl) || CCConst.ENABLED.equals(vc.get(viewControl))) {
	// view.setVisibility(View.VISIBLE);
	// } else {
	// view.setVisibility(View.GONE);
	// }
	// } else if (view instanceof ViewGroup) {
	// disabledObject(context, (ViewGroup) view, vc);
	// } else if (view instanceof ChiaseImageView) {
	// String viewControl = ((ChiaseImageView) view).getViewControl();
	// if (CCConst.ENABLED.equals(viewControl) || CCConst.ENABLED.equals(vc.get(viewControl))) {
	// view.setVisibility(View.VISIBLE);
	// } else {
	// view.setVisibility(View.GONE);
	// }
	// } else if (view instanceof ChiaseCheckBox) {
	// String viewControl = ((ChiaseCheckBox) view).getViewControl();
	// if (CCConst.ENABLED.equals(viewControl) || CCConst.ENABLED.equals(vc.get(viewControl))) {
	// view.setEnabled(true);
	// } else {
	// view.setEnabled(false);
	// }
	// } else if (view instanceof ChiaseSwitch) {
	// String viewControl = ((ChiaseSwitch) view).getViewControl();
	// if (CCConst.ENABLED.equals(viewControl) || CCConst.ENABLED.equals(vc.get(viewControl))) {
	// view.setEnabled(true);
	// } else {
	// view.setEnabled(false);
	// }
	// }
	// }
	// }
	//
	// /**
	// * <strong>disabledObject2</strong><br>
	// * <br>
	// *
	// * @return
	// */
	// public static void disabledObject2(Context context, ViewGroup layout, String viewMode) {
	// if (layout == null) {
	// return;
	// }
	//
	// for (int i = 0; i < layout.getChildCount(); i++) {
	// View view = layout.getChildAt(i);
	// if (view instanceof EditText) {
	// if (CCConst.VIEW_MODE_NEW.equals(viewMode) || CCConst.VIEW_MODE_EDIT.equals(viewMode)) {
	// EditText defaultEditText = new EditText(context);
	// view.setEnabled(true);
	// view.setBackground(defaultEditText.getBackground());
	// ((EditText) view).setCursorVisible(true);
	// ((EditText) view).setKeyListener(defaultEditText.getKeyListener());
	// } else {
	// view.setEnabled(false);
	// view.setBackgroundColor(Color.TRANSPARENT);
	// ((EditText) view).setCursorVisible(false);
	// ((EditText) view).setKeyListener(null);
	// ((EditText) view).setMinHeight(50);
	// }
	// } else if (view instanceof Spinner) {
	// if (CCConst.VIEW_MODE_NEW.equals(viewMode) || CCConst.VIEW_MODE_EDIT.equals(viewMode)) {
	// Spinner defaultSpinner = new Spinner(context);
	// view.setEnabled(true);
	// view.setBackground(defaultSpinner.getBackground());
	// } else {
	// view.setEnabled(false);
	// view.setBackgroundColor(Color.TRANSPARENT);
	// }
	// // } else if (view instanceof Button) {
	// // if (CCConst.ENABLED.equals(viewControl) || CCConst.ENABLED.equals(vc.get(viewControl))) {
	// // view.setVisibility(View.VISIBLE);
	// // } else {
	// // view.setVisibility(View.GONE);
	// // }
	// } else if (view instanceof ViewGroup) {
	// disabledObject2(context, (ViewGroup) view, viewMode);
	// } else if (view instanceof CheckBox) {
	// if (CCConst.VIEW_MODE_NEW.equals(viewMode) || CCConst.VIEW_MODE_EDIT.equals(viewMode)) {
	// view.setEnabled(true);
	// } else {
	// view.setEnabled(false);
	// }
	// } else if (view instanceof Switch) {
	// if (CCConst.VIEW_MODE_NEW.equals(viewMode) || CCConst.VIEW_MODE_EDIT.equals(viewMode)) {
	// view.setEnabled(true);
	// } else {
	// view.setEnabled(false);
	// }
	// }
	// }
	// }

	/**
	 * <strong>disabledCommonButton</strong><br>
	 * <br>
	 *
	 * @return
	 */
}
